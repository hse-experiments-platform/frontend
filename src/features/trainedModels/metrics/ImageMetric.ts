class ImageMetric {
    id: number;
    name: string;
    graphicUrl: string;

    constructor(id: number, name: string, graphicUrl: string) {
        this.id = id;
        this.name = name;
        this.graphicUrl = graphicUrl;
    }
}

export default ImageMetric;